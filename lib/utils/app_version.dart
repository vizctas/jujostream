// Single source of truth for the app version.
// Update here and every screen picks it up automatically.
const String kAppVersion     = '1.0.2';
const int    kAppBuildNumber = 3;
const String kAppVersionDisplay = 'v$kAppVersion — beta';
